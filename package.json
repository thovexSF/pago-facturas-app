{
  "name": "bioma-facturacion",
  "version": "0.3.1",
  "private": true,
  "description": "Bioma Facturación — facturas por pagar + emisión SII Shopify (monorepo Railway).",
  "workspaces": [
    "backend",
    "frontend",
    "apps/pago-facturas"
  ],
  "scripts": {
    "dev": "concurrently -n api,ui -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:pago": "node apps/pago-facturas/server-postgres.js",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend",
    "dev:api": "npm run dev -w backend",
    "dev:ui": "npm run dev -w frontend",
    "dev:shopify": "npm run dev --prefix shopify",
    "postinstall": "npx playwright install chromium || true"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
